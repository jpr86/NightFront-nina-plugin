TODOs

For reference, the template on which this project is based came from here: https://github.com/isbeorn/nina.plugin.template. A similar project to this one is here: https://github.com/tcpalmer/nina.plugin.targetscheduler

1) Look at the circled text in Image-5. The two lines concerning notifications seem to be vestigial artifacts of something else. Delete them. Keep the line above for specifying location of schedule json files.

2) The Container instruction still has a misplaced color icon. Remove it.

3) The color logo should be used for the icon on the NINA plugins page. See Image-6 and notice that other (but not all) plugins have color icons. Let's be professional here and use the one we have for this purpose. Note that this may require specifying this URL: https://github.com/jpr86/NightFront-nina-plugin/blob/master/Resources/NightFrontIcon.png. Also note that the repository is now public.

4) Please verify the rotation angle captured with the metadata for calibration frames is the measured rotation angle of the rotator after slew, center, and rotate of the target, and NOT the input rotation angle of the target, which is the Sky PA. Also, note that the metatdata information should be unique to within 1 degree for the rotation angle. This means that we should not record the same filter and rotation angle (within 1 degree) multiple times. 